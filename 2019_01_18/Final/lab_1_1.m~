clc;
clear;
n=0:64;
=1;
f=1;
fs=8;
subplot(2,1,2);
y=a*sin(2*pi*(f/fs)*n+0);
plot(y);plot(n,y);
xlabel('n');
ylabel('sinx');
title('sinusoidal wave');


n=0:64;
a=1;
f=1;
fs=8;
subplot(2,1,2);
y=a*sin(2*pi*(f/fs)*n+3.14/2);
plot(y);plot(n,y);
xlabel('n');
ylabel('sinx');
title('sinusoidal wave');